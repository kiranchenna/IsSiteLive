DATABASE_URL=sqlite:///./data/issitelive.db
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=replace-me-with-generated-fernet-key
SCREENSHOTS_DIR=./data/screenshots

SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=alerts@issitelive.local
SMTP_USE_TLS=false

DEFAULT_CHECK_CONCURRENCY=3
DEFAULT_STEP_TIMEOUT_MS=15000
